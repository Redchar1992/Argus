# Java / Maven
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# Node
node_modules/
dist/
*.local
coverage/
playwright-report/
test-results/
.vite/

# Env / secrets (NEVER commit real keys)
.env
.env.*
!.env.example

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store

# TS composite build artifacts
*.tsbuildinfo
frontend/*/vite.config.js
frontend/*/vite.config.d.ts

# Local job-search materials; keep resumes and interview strategy out of the
# public project history while still allowing the application pack to live
# beside the working tree.
/application/binance-identity-security/
/docs/interview-prep/

# Locally generated development PKI; never commit private keys/cert bundles.
/infra/tls/generated/
/infra/drills/results/
