# Argus local infrastructure: Postgres (SQL), Mongo (NoSQL trace store), Redis, and opt-in Prometheus.
#
# Quick start (databases only — services run from your IDE / java -jar):
#   docker compose up -d postgres mongo redis
#
# The Spring services default to in-memory H2 + in-memory trace store, so this
# compose file is OPTIONAL for a demo. Use it to exercise the real SQL + NoSQL
# path:
#   SPRING_PROFILES_ACTIVE=postgres   (auth/tools/case services)
#   SPRING_PROFILES_ACTIVE=mongo + ARGUS_TRACE_STORE=mongo  (orchestrator)

services:
  postgres:
    image: postgres:16-alpine
    container_name: argus-postgres
    environment:
      POSTGRES_USER: argus
      POSTGRES_PASSWORD: argus
      POSTGRES_DB: argus
    ports:
      - "5432:5432"
    volumes:
      - ./infra/postgres:/docker-entrypoint-initdb.d:ro
      - argus-pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U argus"]
      interval: 5s
      timeout: 5s
      retries: 10

  mongo:
    image: mongo:7
    container_name: argus-mongo
    ports:
      - "27017:27017"
    volumes:
      - ./infra/mongo:/docker-entrypoint-initdb.d:ro
      - argus-mongo:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: argus-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  # Opt-in local security fixture. Generate short-lived certificates first with
  # ./infra/tls/generate-dev-pki.sh, then run: docker compose --profile security up -d redis-secure
  redis-secure:
    profiles: ["security"]
    image: redis:7-alpine
    container_name: argus-redis-secure
    user: "0:0"
    environment:
      ARGUS_DEV_REDIS_PASSWORD: ${ARGUS_DEV_REDIS_PASSWORD:-argus-dev-redis-secret}
    command:
      - redis-server
      - --port
      - "0"
      - --tls-port
      - "6380"
      - --tls-cert-file
      - /tls/redis-server.crt
      - --tls-key-file
      - /tls/redis-server.key
      - --tls-ca-cert-file
      - /tls/ca.crt
      - --tls-auth-clients
      - "yes"
      - --requirepass
      - ${ARGUS_DEV_REDIS_PASSWORD:-argus-dev-redis-secret}
    ports:
      - "6380:6380"
    volumes:
      - ./infra/tls/generated:/tls:ro
      - argus-redis-secure:/data
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          REDISCLI_AUTH=$${ARGUS_DEV_REDIS_PASSWORD:-argus-dev-redis-secret}
          redis-cli --tls -h localhost -p 6380 --cacert /tls/ca.crt
          --cert /tls/bff-redis-client.crt --key /tls/bff-redis-client.key ping
      interval: 5s
      timeout: 5s
      retries: 10

  redis-drill-primary:
    profiles: ["drill"]
    image: redis:7-alpine
    container_name: argus-redis-drill-primary
    # Match the caller so mode-0600 bind-mounted development keys remain private and readable
    # on native Linux as well as Docker Desktop.
    user: "${ARGUS_DRILL_CONTAINER_UID:-1000}:${ARGUS_DRILL_CONTAINER_GID:-1000}"
    environment:
      ARGUS_DRILL_REDIS_PASSWORD: ${ARGUS_DRILL_REDIS_PASSWORD:-argus-drill-redis-secret}
    command:
      - redis-server
      - --port
      - "0"
      - --tls-port
      - "6379"
      - --tls-cert-file
      - /tls/redis-drill-primary.crt
      - --tls-key-file
      - /tls/redis-drill-primary.key
      - --tls-ca-cert-file
      - /tls/ca.crt
      - --tls-auth-clients
      - "yes"
      - --requirepass
      - ${ARGUS_DRILL_REDIS_PASSWORD:-argus-drill-redis-secret}
      - --dir
      - /tmp
      - --appendonly
      - "yes"
      - --appendfsync
      - "always"
    ports:
      - "6391:6379"
    volumes:
      - ./infra/tls/generated:/tls:ro
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          REDISCLI_AUTH=$${ARGUS_DRILL_REDIS_PASSWORD:-argus-drill-redis-secret}
          redis-cli --tls -h localhost -p 6379 --cacert /tls/ca.crt
          --cert /tls/bff-redis-client.crt --key /tls/bff-redis-client.key ping
      interval: 2s
      timeout: 2s
      retries: 20

  redis-drill-replica:
    profiles: ["drill"]
    image: redis:7-alpine
    container_name: argus-redis-drill-replica
    user: "${ARGUS_DRILL_CONTAINER_UID:-1000}:${ARGUS_DRILL_CONTAINER_GID:-1000}"
    depends_on:
      redis-drill-primary:
        condition: service_healthy
    environment:
      ARGUS_DRILL_REDIS_PASSWORD: ${ARGUS_DRILL_REDIS_PASSWORD:-argus-drill-redis-secret}
    command:
      - redis-server
      - --port
      - "0"
      - --tls-port
      - "6379"
      - --tls-cert-file
      - /tls/redis-drill-replica.crt
      - --tls-key-file
      - /tls/redis-drill-replica.key
      - --tls-ca-cert-file
      - /tls/ca.crt
      - --tls-auth-clients
      - "yes"
      - --tls-replication
      - "yes"
      - --replicaof
      - redis-drill-primary
      - "6379"
      - --masterauth
      - ${ARGUS_DRILL_REDIS_PASSWORD:-argus-drill-redis-secret}
      - --requirepass
      - ${ARGUS_DRILL_REDIS_PASSWORD:-argus-drill-redis-secret}
      - --dir
      - /tmp
      - --appendonly
      - "yes"
      - --appendfsync
      - "always"
    ports:
      - "6392:6379"
    volumes:
      - ./infra/tls/generated:/tls:ro
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          REDISCLI_AUTH=$${ARGUS_DRILL_REDIS_PASSWORD:-argus-drill-redis-secret}
          redis-cli --tls -h localhost -p 6379 --cacert /tls/ca.crt
          --cert /tls/bff-redis-client.crt --key /tls/bff-redis-client.key ping
      interval: 2s
      timeout: 2s
      retries: 20

  prometheus:
    profiles: ["monitoring"]
    image: prom/prometheus:v3.13.1
    container_name: argus-prometheus
    user: "${ARGUS_PROMETHEUS_UID:-65534}:${ARGUS_PROMETHEUS_GID:-65534}"
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/tmp/prometheus
      - --storage.tsdb.retention.time=24h
    ports:
      - "9090:9090"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ${ARGUS_PROMETHEUS_CONFIG:-./infra/monitoring/prometheus.yml}:/etc/prometheus/prometheus.yml:ro
      - ./infra/monitoring/identity-alerts.yml:/etc/prometheus/identity-alerts.yml:ro
      - ./infra/tls/generated:/etc/prometheus/tls:ro
    healthcheck:
      test: ["CMD", "promtool", "check", "ready"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  argus-pg:
  argus-mongo:
  argus-redis-secure:
